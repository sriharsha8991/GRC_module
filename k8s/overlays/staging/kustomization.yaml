apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: grc-staging

resources:
  - ../../base
  - namespace.yaml

secretGenerator:
  - name: grc-secrets
    envs:
      - secrets.env

generatorOptions:
  disableNameSuffixHash: true

patches:
  - path: patches/resources.yaml
